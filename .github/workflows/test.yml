name: Test
on:
  push:
    branches:
      - master
      - preview
      - ci
      - release/v2.0
  pull_request:
    branches:
      - master
      - preview
jobs:
  publish:
    name: Publish
    if: github.head_ref == 0
    runs-on: ubuntu-latest
    steps:
      - shell: pwsh
        run: |
          "I Ran!: ${{github.head_ref}} "
